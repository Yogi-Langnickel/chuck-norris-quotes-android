package com.yogi.chucknorris

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * ExampleUnitTest is a unit test class that tests the functionality of the ViewModel or other components.
 */
class ExampleUnitTest {

    /**
     * Example test to check if addition is correct.
     */
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}